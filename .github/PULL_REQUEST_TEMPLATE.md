## Description

<!-- What does this change do, and why? -->

## Checklist

- [ ] `pnpm run check` passes (Biome lint + format)
- [ ] `pnpm run typecheck` passes (`astro check`)
- [ ] `pnpm build` succeeds
- [ ] Screenshot attached, if this changes the UI
